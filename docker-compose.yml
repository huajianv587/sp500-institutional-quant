services:
  institutional-quant:
    build: .
    env_file:
      - .env
    environment:
      IQ_HOST: 0.0.0.0
    volumes:
      - institutional_quant_data:/home/appuser/app/data
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped

volumes:
  institutional_quant_data:
